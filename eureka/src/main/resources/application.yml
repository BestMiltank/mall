server:
  port: 8761

eureka:
  instance:
    ip-address: 47.103.202.218
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.ip-address}:${server.port}/eureka